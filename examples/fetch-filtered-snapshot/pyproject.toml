[project]
name = "nyc311-example-fetch-filtered-snapshot"
version = "0.0.0"
description = "Self-contained nyc311 filtered snapshot example."
requires-python = ">=3.10"
dependencies = ["nyc311"]

[tool.uv.sources]
nyc311 = { path = "../..", editable = true }
