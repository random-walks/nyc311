[project]
name = "nyc311-example-boundary-qa"
version = "0.0.0"
description = "Self-contained nyc311 boundary QA example."
requires-python = ">=3.10"
dependencies = ["nyc311[spatial,plotting]"]

[tool.uv.sources]
nyc311 = { path = "../..", editable = true }
