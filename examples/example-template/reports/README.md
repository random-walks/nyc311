# Tracked Reports

When a copied example produces stable, useful outputs that belong in git, place
them here.

Recommended layout:

- `reports/<example-slug>-tearsheet.md`
- `reports/figures/<report-figure>.png`

Markdown should reference figures with explicit relative paths such as:

```md
![Example chart](./figures/example-chart.png)
```
